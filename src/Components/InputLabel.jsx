import React from "react";

function InputLabel(props) {
    return (
        <h5 className="text-xl font-serif text-gray-700 font-semibold">{props.children }</h5>
    );

}

export default InputLabel;